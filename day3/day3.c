#include <stdio.h>
#define MAX 8192

int main(int argc, char * argv[]) {
    int coord[MAX][2];
    int total = 1;
    int c
    //start at coordinates (0,0)
    //with each ^><v coordinates increase or decrease by one
    //we remember each coordinates and if it not in memory then 
    //we increment house with at least one present by one
    //how do we store the memory of the two numbers tho
    //maybe an array of array with two 
}
